component = data + logic + html template
module = container or grp of related components

ng g c name --creates component
ng g s name --creates service

string interpolation
dependecy injection 
singelton pattern

ngFor 
ngIf or ([hidden] for small trees)
ngSwitchCase 
ngClass
ngStyle
ngModel

directives-> to modify the dom
types
    ->Stuctural (with astrics *)
    ->Attribute
custom directives

two-way Binding using ngModel from forms

template driven forms raective forms

angular material